#!/bin/lua


--- package
-- data
_g1_pack_data = {
        [[--ro-bind "${HOME}/.config/user-dirs.dirs" "${HOME}/.config/user-dirs.dirs"]],
        [[--ro-bind "${HOME}/.config/user-dirs.locale" "${HOME}/.config/user-dirs.locale"]],
}

